# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Wails / Go build output. Everything under build/ is generated: the binary, the staged
# Tesseract bundle and the downloaded installer cache. The icon, manifest and version
# template Wails reads from build/ are sources and live in packaging/, which build.ps1
# copies in before every build.
build/
*.exe
*.syso

# Generated by the Wails CLI on every build — see CLAUDE.md "Building"
frontend/wailsjs/
frontend/package.json.md5
